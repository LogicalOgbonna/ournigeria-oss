{
  "name": "api",
  "projectType": "application",
  "sourceRoot": "apps/api/src",
  "targets": {
    "dev": {
      "command": "infisical run --env dev --watch -- nest start --watch",
      "options": {
        "cwd": "apps/api"
      }
    },
    "build": {
      "command": "nest build",
      "options": {
        "cwd": "apps/api"
      }
    },
    "start": {
      "command": "infisical run --env prod --watch -- nest start",
      "options": {
        "cwd": "apps/api"
      }
    },
    "prisma:generate": {
      "command": "infisical run -- prisma generate",
      "options": {
        "cwd": "apps/api"
      }
    }
  }
}
