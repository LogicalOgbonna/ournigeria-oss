"use client";

/**
 * Official-profile design experiment — the selected layout (V9 "Magazine Profile").
 * Editorial longform with structured, evidence-backed sections, kept here on mock
 * data as the reference until it's wired to the live officials API and promoted to
 * the real /officials/[slug] route (plan 45f).
 */

import { VariantH } from "./variants/VariantH";

export default function OfficialProfileExperiment() {
  return <VariantH />;
}
