{
  "name": "@ournigeria/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Python download and scraping scripts",
  "scripts": {
    "add-corruption-sources": "infisical run --projectId ${INFISICAL_PROJECT_ID:-INFISICAL_PROJECT_ID} --env dev -- node add-corruption-sources/add-corruption-sources.mjs",
    "populate-corruption-cases": "node populate-corruption-cases/populate-corruption-cases.mjs"
  },
  "dependencies": {
    "@ournigeria/access": "workspace:*",
    "pg": "^8.13.0",
    "playwright": "1.58.2"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0"
  }
}
