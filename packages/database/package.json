{
  "name": "@ournigeria/database",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "infisical run -- prisma migrate deploy",
    "prisma:migrate:create": "infisical run -- npx tsx scripts/create-migration.ts",
    "prisma:studio": "infisical run -- prisma studio",
    "seed:party-chapters": "infisical run -- npx tsx scripts/seed-party-chapters.ts",
    "seed:party-profiles": "infisical run -- npx tsx scripts/seed-party-profiles.ts",
    "seed:party-officers": "infisical run -- npx tsx scripts/seed-party-officers.ts",
    "seed:party-candidates": "infisical run -- npx tsx scripts/seed-party-candidates.ts",
    "seed:governor-defections": "infisical run -- npx tsx scripts/seed-governor-defections.ts",
    "seed:ndc-defectors": "infisical run -- npx tsx scripts/seed-ndc-defectors.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "@nestjs/common": "catalog:",
    "pg": "catalog:",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "prisma": "catalog:",
    "typescript": "catalog:",
    "@types/pg": "catalog:"
  }
}
