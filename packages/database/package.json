{
  "name": "@ournigeria/database",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "test": "vitest run",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "infisical run --path /api -- prisma migrate deploy",
    "prisma:migrate:create": "infisical run --path /api -- npx tsx scripts/create-migration.ts",
    "prisma:studio": "infisical run --path /api -- prisma studio",
    "seed:party-chapters": "infisical run --path /api -- npx tsx scripts/seed-party-chapters.ts",
    "seed:party-profiles": "infisical run --path /api -- npx tsx scripts/seed-party-profiles.ts",
    "seed:party-officers": "infisical run --path /api -- npx tsx scripts/seed-party-officers.ts",
    "seed:party-candidates": "infisical run --path /api -- npx tsx scripts/seed-party-candidates.ts",
    "seed:governor-defections": "infisical run --path /api -- npx tsx scripts/seed-governor-defections.ts",
    "seed:ndc-defectors": "infisical run --path /api -- npx tsx scripts/seed-ndc-defectors.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "@nestjs/common": "catalog:",
    "pg": "catalog:",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "prisma": "catalog:",
    "typescript": "catalog:",
    "@types/pg": "catalog:",
    "vitest": "catalog:"
  }
}
