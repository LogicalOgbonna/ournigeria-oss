{
  "name": "database",
  "projectType": "library",
  "sourceRoot": "packages/database/src",
  "targets": {
    "build": {
      "command": "prisma generate && tsc",
      "cache": false,
      "options": {
        "cwd": "packages/database"
      }
    },
    "prisma:generate": {
      "command": "prisma generate",
      "options": {
        "cwd": "packages/database"
      }
    },
    "prisma:migrate": {
      "command": "infisical run --path /api -- prisma migrate deploy",
      "options": {
        "cwd": "packages/database"
      }
    },
    "prisma:studio": {
      "command": "infisical run --path /api -- prisma studio",
      "options": {
        "cwd": "packages/database"
      }
    }
  }
}
