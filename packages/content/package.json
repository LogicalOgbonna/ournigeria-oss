{
  "name": "@ournigeria/content",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "main": "image-service.ts",
  "exports": {
    ".": "./image-service.ts",
    "./templates": "./templates/index.ts"
  },
  "scripts": {
    "generate": "infisical run --projectId ${INFISICAL_PROJECT_ID:-INFISICAL_PROJECT_ID} --env dev -- npx tsx generate.ts",
    "query": "infisical run --projectId ${INFISICAL_PROJECT_ID:-INFISICAL_PROJECT_ID} --env dev -- npx tsx query.ts",
    "render": "npx tsx render.ts",
    "engine": "infisical run --projectId ${INFISICAL_PROJECT_ID:-INFISICAL_PROJECT_ID} --env dev -- npx tsx engine.ts",
    "batch": "infisical run --projectId ${INFISICAL_PROJECT_ID:-INFISICAL_PROJECT_ID} --env dev -- npx tsx batch.ts",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@napi-rs/canvas": "0.1.80",
    "@resvg/resvg-js": "2.6.2",
    "openai": "4.104.0",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@types/pg": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
