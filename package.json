{
  "name": "@naija-budget/root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "web:dev": "nx run web:dev",
    "web:build": "nx run web:build",
    "web:start": "nx run web:start",
    "web:lint": "nx run web:lint",
    "prisma:generate": "nx run web:prisma:generate",
    "prisma:migrate": "nx run web:prisma:migrate",
    "prisma:studio": "nx run web:prisma:studio",
    "prisma:seed": "nx run web:prisma:seed",
    "prisma:reset": "nx run web:prisma:reset",
    "prisma:reset:force": "nx run web:prisma:reset:force",
    "prisma:reset:force:with-seed": "nx run web:prisma:reset:force:with-seed",
    "prisma:reset:force:with-seed:and-migrate": "nx run web:prisma:reset:force:with-seed:and-migrate",
    "ingest": "nx run web:ingest",
    "ingest:corruption": "nx run web:ingest:corruption",
    "api:dev": "nx run api:dev",
    "api:build": "nx run api:build",
    "api:start": "nx run api:start",
    "api:prisma:generate": "nx run api:prisma:generate",
    "ingest:dev": "nx run ingest:dev",
    "ingest:build": "nx run ingest:build",
    "ingest:start": "nx run ingest:start"
  },
  "devDependencies": {
    "nx": "^21.3.2",
    "@nx/js": "^21.3.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "esbuild",
      "lightningcss",
      "nx",
      "prisma",
      "sharp",
      "tesseract.js",
      "unrs-resolver"
    ]
  }
}
