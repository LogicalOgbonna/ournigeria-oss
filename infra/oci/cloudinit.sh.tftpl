#!/usr/bin/env bash
# placeholder — real provisioning script authored in Task 6
# template vars referenced so validate succeeds:
# ${infisical_client_id} ${infisical_client_secret} ${infisical_project_id} ${repo_url} ${repo_ref}
echo placeholder
